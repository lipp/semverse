/**
 * ### Environment configuration
 *
 * Template for all environments.
 * This is a subset of {@link module:Configuration/Default} with properties
 * that have to be overloaded for each environment
 *
 * @module Configuration/Environment
 */
//'use strict';
//module.exports = {};
